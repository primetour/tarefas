{
  "rules": {
    "presence": {
      ".read": "auth !== null",
      "$uid": {
        ".write": "auth !== null && auth.uid === $uid",
        ".validate": "newData.hasChildren(['uid', 'name', 'lastSeen']) && newData.child('uid').val() === $uid"
      }
    }
  }
}
